fn fizzbuzz(n) -> {
    if n % 15 == 0 then
        "FizzBuzz"
    else if n % 3 == 0 then
        "Fizz"
    else if n % 5 == 0 then
        "Buzz"
    else
        n
}

fn fizzbuzz_run(s,e) -> {
    if s == e then
        e
    else
       _ = print(fizzbuzz(s)) in fizzbuzz_run(s+1,e)
}

fizzbuzz_run(1,101)
print("hello")